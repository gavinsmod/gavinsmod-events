{
  "schemaVersion": 1,
  "id": "gavinsmod-events",
  "version": "${version}",
  "name": "gavinsmod-events",
  "description": "An event library for fabric mods.",
  "authors": [],
  "contact": {
    "repo": "https://github.com/gcpease/gavinsmod-events"
  },
  "mixins": [
    "gavinsmod-events.mixins.json"
  ],
  "license": "MIT",
  "icon": "assets/gavinsmod-events/icon.png",
  "environment": "*",
  "entrypoints": {

  },
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
